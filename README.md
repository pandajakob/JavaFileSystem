# File tree viewer in Java

- View all files in the folder you are running the executable from. 
- All strings in args will be omitted
- "-d" will set the max depth. For example -d 2 will set the max depth to 2
